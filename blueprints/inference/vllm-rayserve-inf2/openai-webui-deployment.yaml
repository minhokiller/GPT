#-----------------------------------------------------------------------------
# Deployment Instructions
# kubectl apply -f gen-ai/inference/nvidia-nim/openai-webui-deployment.yaml
# kubectl port-forward svc/open-webui 8080:80 -n openai-webui
# Open WebUi using http://localhost:8080
#-----------------------------------------------------------------------------
apiVersion: v1
kind: Namespace
metadata:
  name: openai-webui

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: open-webui
  namespace: openai-webui
  labels:
    app: open-webui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: open-webui
  template:
    metadata:
      labels:
        app: open-webui
    spec:
      containers:
      - name: open-webui
        image: ghcr.io/open-webui/open-webui:main
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "1000m"
            memory: "2Gi"
        env:
        # This is the vllm k8s service URL to which Open-WebUI client connects to.
        - name: OPENAI_API_BASE_URLS
          value: "http://vllm-llama3-inf2-serve-svc.vllm.svc.cluster.local:8000/v1"
        - name: OPENAI_API_KEY
          value: "dummy" # Replace with actual API key if required
        volumeMounts:
        - name: webui-volume
          mountPath: /app/backend/data
      volumes:
      - name: webui-volume
        emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: openai-webui
  labels:
    app: open-webui
spec:
  type: ClusterIP
  selector:
    app: open-webui
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: open-webui
  namespace: openai-webui
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: "/$1"
spec:
  ingressClassName: nginx
  rules:
  - host: k8s-ingressn-ingressn-8f49a9a029-27389f8cca29312e.elb.us-east-1.amazonaws.com
    http:
      paths:
      - path: /(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: open-webui
            port:
              number: 80